/superpowers:brainstorming

Work on ticket {TICKET}: {URL}

An orchestrator is watching this ticket's tracker status to spawn downstream work. Update the status as you progress (start → in progress → in review → done) so the pipeline moves.
